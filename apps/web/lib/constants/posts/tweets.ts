import { type PostSummary } from "../../get-posts";

const posts: PostSummary[] = [
  { title: "Tweets", description: "My Tweets", slug: "" },
];

// eslint-disable-next-line import/no-default-export
export default posts;
